h3.light-bottom
    icon question
    | What Am I Doing Here?

div(ng-controller='supportController')

    .lead Add a question or comment here and a FTSS support team member will get back to you pretty quick.

    .panel.panel-default(ng-repeat='comment in comments')
        .panel-body {{comment.Comment}}
        .panel-footer(ng-class='{bold:comment.Staff}')
            .pull-left {{comment.CreatedBy.Name}}
            .pull-right {{comment.TimeAgo}}
